<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Edit Component</title>
    <style>
        body {
            font-family: Arial, sans-serif;
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            background-color: white;
            color: black;
        }

        header {
            background-color: black;
            color: white;
            padding: 10px;
            text-align: center;
        }

        h1 {
            margin: 0;
        }

        form {
            text-align: center;
            margin: 20px;
        }

        label {
            display: block;
            margin-bottom: 5px;
            color: black; /* Change to your preferred color */
        }

        input[type="text"], textarea {
            width: 100%;
            padding: 10px;
            margin-bottom: 20px;
            box-sizing: border-box;
        }

        /* Add this CSS for side-by-side layout */
        .flex-container {
            display: flex;
            justify-content: space-between;
            gap: 20px;
        }

        .flex-item {
            flex: 1;
        }

        
        .flex-item-label {
            flex: 1; /* This makes the label take less space */
        }

        .flex-item-input {
            flex: 3; /* This makes the input take more space */
        }

        button {
            padding: 10px;
            background-color: black; /* Change to your preferred color */
            color: white;
            border: none;
            cursor: pointer;
            transition: background-color 0.3s, color 0.3s;
            border: 1px solid white;
        }

        button:hover {
            background-color: white;
            color: black;
            border: 1px solid black;
        }

        footer {
            background-color: black; /* Change to your preferred color */
            color: white;
            padding: 10px;
            text-align: center;
        }
    </style>
</head>
<body>
    <header>
        <h1>Software Component Cataloging Software</h1>
    </header>

    <center><h1 style="margin-top: 2%;"><i>Edit Component</i></h1></center>
    
<form action="/editComponent/<%= component._id %>" method="post">
    <div class="flex-container">
        <div class="flex-item flex-item-label">
            <label for="title">Title:</label>
        </div>
        <div class="flex-item flex-item-input">
            <input type="text" id="title" name="title" value="<%= component.title %>" required>
        </div>
    </div>

    <div class="flex-container">
        <div class="flex-item flex-item-label">
            <label for="tag">Tag:</label>
        </div>
        <div class="flex-item flex-item-input">
            <input type="text" id="tag" name="tag" value="<%= component.tag %>" required>
        </div>
    </div>

    <div class="flex-container">
        <div class="flex-item flex-item-label">
            <label for="description">Description:</label>
        </div>
        <div class="flex-item flex-item-input">
            <input type="text" id="description" name="description" value="<%= component.description %>" required>
        </div>
    </div>

    <div class="flex-container">
        <div class="flex-item flex-item-label">
            <label for="code">Code:</label>
        </div>
        <div class="flex-item flex-item-input">
            <textarea id="code" name="code" rows="10" required><%= component.code %></textarea>
        </div>
    </div>

    <button type="submit">Save Changes</button>
</form>
    

    <footer style="margin-top: 7%;">
        &copy; 2023 Software Component Cataloging Software
    </footer>
</body>
</html>
